// Thin re-export stub — daily log types have moved to @/features/workspace.slice.
// This file is kept for backward compatibility with existing @/shared/types imports.
export type { DailyLogComment, DailyLog } from '@/features/workspace.slice'
