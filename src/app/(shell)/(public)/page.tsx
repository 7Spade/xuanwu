/**
 * Module: public/page
 * Purpose: Map `/` to the public landing route tree.
 * Responsibilities: Ensure root path participates in (public) parallel modal routes.
 * Constraints: deterministic logic, respect module boundaries
 */
export { default } from "./landing/page";
