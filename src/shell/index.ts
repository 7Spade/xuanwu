/**
 * src/shell — Public API
 *
 * Application shell assembly layer.
 * Exports the authenticated application shell and its chrome components.
 *
 * External consumers import from '@/shell'.
 */

export { AppShell } from './app-shell';
