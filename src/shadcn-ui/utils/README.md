# src/shadcn-ui/utils

shadcn/ui 配套工具函式。供所有組件與功能層引用。

## Files

| File | Exports |
|------|---------|
| `utils.ts` | `cn` — Tailwind class merger（clsx + twMerge） |
| | `firestoreTimestampToISO` — Firestore Timestamp → ISO 8601 字串 |
| | `hexToHsl` — Hex 色碼 → HSL CSS 變數格式 |

## Usage

```ts
import { cn } from "@/shadcn-ui/utils/utils"
```
